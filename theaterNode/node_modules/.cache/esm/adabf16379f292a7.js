_79f‍.x([["default",()=>_79f‍.o]]);const {Sequelize, Model} = require('sequelize');
const sequelize = require("../helpers/sequelize");

class Theater extends Model {}

Theater.init({
    id: {
        type: Sequelize.BIGINT.UNSIGNED,
        primaryKey: true,
        allowNull: false,
        autoIncrement: true,
    },

    name: {
        type: Sequelize.STRING,
        allowNull: false,
        validate: {
            isAlpha: {
                msg: "Invalid First Name"
            }
        }
    },

    rows: {
        type: Sequelize.STRING,
        allowNull: false,
        validate: {
            isAlpha: {
                msg: "Invalid First Row"
            }
        }
    },

    cols: {
        type: Sequelize.STRING,
        allowNull: false,
        validate: {
            isAlpha: {
                msg: "Invalid First Col"
            }
        }
    }

}, {
    sequelize,
    modelName: 'theater',
    timestamps: false,
});

Theater.sync();

_79f‍.d(Theater);